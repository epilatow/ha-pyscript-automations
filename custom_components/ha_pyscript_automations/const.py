# This is AI generated code
"""Constants for the ha-pyscript-automations integration."""

from __future__ import annotations

DOMAIN = "ha_pyscript_automations"

# Persisted manifest of installed destination paths so the
# next reconcile knows what we own and what to remove when
# a bundled file disappears between releases. Stored via
# homeassistant.helpers.storage.Store at .storage/<key>.
STORAGE_KEY = f"{DOMAIN}.installed"
STORAGE_VERSION = 1

# Options-flow key for the optional CLI symlink dir.
OPTION_CLI_SYMLINK_DIR = "cli_symlink_dir"
