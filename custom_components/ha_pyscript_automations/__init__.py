# This is AI generated code
"""ha-pyscript-automations integration package.

Intentionally empty at this stage of the HACS migration.
The real integration entry points (async_setup_entry,
async_unload_entry, async_remove_entry) land in a later
commit along with manifest.json and the config flow. For
now this module exists only so the reconciler and installer
modules are importable as a package from the dev-install CLI
and from tests.
"""
